import type {RepairStatus,RepairPriority} from '../types'; export const REPAIR_STATUS:Record<Uppercase<RepairStatus>,RepairStatus>={PENDING:'pending',ASSIGNED:'assigned',PROCESSING:'processing',DONE:'done',CLOSED:'closed'}; export const repairStatusText:Record<RepairStatus,string>={pending:'待受理',assigned:'已分派',processing:'处理中',done:'已完成',closed:'已关闭'}; export const REPAIR_PRIORITY:Record<Uppercase<RepairPriority>,RepairPriority>={NORMAL:'normal',URGENT:'urgent',MAJOR:'major'}; export const repairPriorityText:Record<RepairPriority,string>={normal:'普通',urgent:'紧急',major:'重大'}; export const repairPriorityDeadlineText:Record<RepairPriority,string>={normal:'24小时内响应',urgent:'8小时内响应',major:'2小时内响应'}; export function formatDuration(seconds:number):string{if(!seconds||seconds<0)return'-';if(seconds<60)return`${seconds}秒`;const mins=Math.floor(seconds/60);if(mins<60)return`${mins}分钟`;const hours=Math.floor(mins/60);const restMin=mins%60;if(hours<24)return restMin?`${hours}小时${restMin}分钟`:`${hours}小时`;const days=Math.floor(hours/24);const restHour=hours%24;return restHour?`${days}天${restHour}小时`:`${days}天`}
